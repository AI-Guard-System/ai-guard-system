{
  "name": "react-ai-guard-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "publish:all": "pnpm -r publish --access public"
  },
  "devDependencies": {
    "pnpm": "^8.0.0"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
